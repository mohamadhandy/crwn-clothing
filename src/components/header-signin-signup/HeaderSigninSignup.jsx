import React from "react";

const HeaderSigninSignup = ({ title, subTitle }) => {
  return (
    <>
      <h2>{title}</h2>
      <span>{subTitle}</span>
    </>
  );
};

export default HeaderSigninSignup;
